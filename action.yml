name: 'issue post'
inputs:
  url:
    description: 'issues url'
    required: true
    default: ${{ github.event.repo.url }}
outputs:
  time: # id of output
    description: 'The time we greeted you'
runs:
  using: 'node12'
  main: 'dist/index.js'
